import { HashLoader } from 'react-spinners';

const HashLoading = () => {
    return (
        <div className='flex justify-center'>
            <HashLoader />
        </div>
    );
};

export default HashLoading;